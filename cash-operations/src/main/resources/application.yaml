spring:
  application:
    name: cash-operations
  datasource:
    url: jdbc:sqlserver://localhost\SQLEXPRESS;databaseName=BankingDB;encrypt=false;trustServerCertificate=true
    username: sa
    password: Tshepo@123
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver
  sql:
    init:
      mode: always
  jpa:
    defer-datasource-initialization: true
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.SQLServerDialect
